name: Sentinel CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.x"
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: Compile Midnight Contract
        run: npm run compact-compiler

      - name: Build Next.js App
        run: npm run build
